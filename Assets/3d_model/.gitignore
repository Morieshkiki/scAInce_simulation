# The road, curb and sidewalk textures in this folder are referenced by
# RoadNetworkRoot and GeneratedSidewalks in MainScene, so they are tracked.
# The large tum2twin campus tiles are excluded from the repository root
# .gitignore instead.
